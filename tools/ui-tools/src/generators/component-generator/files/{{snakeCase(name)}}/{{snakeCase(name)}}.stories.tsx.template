import type { Meta, StoryObj } from '@storybook/react';
import { <%= name %> } from './<%= kebabCase(name) %>';

const Story: Meta<typeof <%= name %>> = {
  component: <%= name %>,
  title: 'Components/UI/<%= name %>',
};

export default Story;

export const Primary: StoryObj<typeof <%= name %>> = {
  args: {},
};



